meta {
  name: Listen to many topics (SSE)
  seq: 10
  type: http
}

get {
  auth: inherit
  body: none
  url: {{baseUrl}}/listen?topics={{topic}}
}

headers {
  Accept: text/event-stream
}

params:query {
  ~since: all
  topics: {{topic}}
}

docs {
  `topics` is a comma-separated list. Omit it to stream every topic the
  authenticated user owns or subscribes to.
}
