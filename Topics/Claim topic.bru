meta {
  name: Claim topic
  seq: 6
  type: http
}

post {
  auth: inherit
  body: json
  url: {{baseUrl}}/topics/{{topic}}/claim
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "accessMode": "private"
  }
}

docs {
  Takes ownership of an unclaimed topic. Returns 409 `already_claimed` if it
  already has an owner.
}
