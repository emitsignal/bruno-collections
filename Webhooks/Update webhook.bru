meta {
  name: Update webhook
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/webhooks/{{webhookId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "github pushes (renamed)",
    "status": "paused",
    "topicName": "{{topic}}",
    "template": null
  }
}
