meta {
  name: Subscription metrics
  seq: 7
  type: http
}

get {
  auth: inherit
  body: none
  url: {{baseUrl}}/subscriptions/metrics?deviceId={{deviceId}}
}

params:query {
  deviceId: {{deviceId}}
}
