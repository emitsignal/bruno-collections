meta {
  name: Unsubscribe
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/subscriptions
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "deviceId": "{{deviceId}}",
    "topicName": "{{topic}}"
  }
}
