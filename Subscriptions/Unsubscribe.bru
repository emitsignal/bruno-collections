meta {
    name: Unsubscribe
    seq: 4
    type: http
}

delete {
    auth: inherit
    body: json
    url: {{baseUrl}}/subscriptions
}

headers {
    Content-Type: application/json
}

body:json {
  {
      "deviceId": "{{deviceId}}",
      "topicName": "{{topic}}"
  }
}
