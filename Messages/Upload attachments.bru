meta {
  name: Upload attachments
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/messages/{{messageId}}/attachments
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  files: @file()
}

docs {
  Multipart upload under the field name `files` (repeat it for several files).
  Point the file picker at something local before sending.
}
